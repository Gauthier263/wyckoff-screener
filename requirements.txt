ccxt>=4.0
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
yfinance>=0.2   # screener multi-cours : actions + matières premières (screener.scan)
pyarrow>=12.0   # cache parquet (optionnel)
matplotlib>=3.7 # graphiques (mode --chart / screener.plot)
pytest>=7.0     # tests
